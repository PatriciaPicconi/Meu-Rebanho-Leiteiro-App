import 'package:flutter/material.dart';

class TelaConsulta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Consulta")),
      body: Center(child: Text("Tela de Consulta")),
    );
  }
}